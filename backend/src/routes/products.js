import { Router } from 'express';

const router = Router();

// GET  /api/products          — Stage 3
// GET  /api/products/:id      — Stage 3
// POST /api/listings          — Stage 3
// DELETE /api/products/:id    — Stage 3

export default router;
